<%= form_for(@selector) do |f| %>
  <% if @selector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selector.errors.count, "error") %> prohibited this selector from being saved:</h2>

      <ul>
      <% @selector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_id %><br>
    <%= f.select(:source_id, [''] + Source.all.collect{|s| s.id}) %>
  </div>
  <div class="field">
    <%= f.label :host %><br>
    <%= f.text_field :host %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
